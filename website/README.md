# Agent Notch website

Static marketing site for Agent Notch.

## Preview locally

```sh
cd website
python3 -m http.server 8080
```

Then open `http://localhost:8080`.

The site deliberately uses no frontend framework or build step. GitHub Pages can publish the `website/` directory directly via the included workflow.
